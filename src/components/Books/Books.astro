---
import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import { Image } from "astro:assets";
import BooksTriggers from "./BooksTriggers";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section class="h-screen m-auto w-full flex items-center justify-center">
  <div class="flex flex-col p-4 py-16 xl:flex-row gap-7 xl:p-0 items-center justify-center">
    <BooksTriggers client:load />
  </div>
</section>
